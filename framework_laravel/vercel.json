{
  "version": 2,
  "buildCommand": "composer install --no-dev && npm install && npm run build && php artisan config:cache",
  "routes": [
    {
      "src": "/(.*)$",
      "dest": "/api/index.php"
    }
  ]
}
